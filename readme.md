# Local dev tool/script

## Redis-cluster

Script to run and operate redis cluster at local

## Monitoring

Dockerfile to run grafana, prometheus 

## Mongodb

```bash
docker compose -p mongodb -f docker-compose.yml up -d
```
